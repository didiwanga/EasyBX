# B4 别名（定稿）

## 别名
每条别名：名称（触发前缀）+ 命令（展开内容）。

## 处理
- 输入栏输入别名前缀 → 展开为对应命令再发送。
- **优先级**（与 B6 输入栏补全一致）：历史 → 命令字典 → 方向 → 别名。别名最末，且仅当 Tab 补全无命中时再走别名展开（详见 B6「Tab 补全」）。

## 目标
- 游戏内 `alias/cemote` 是 MUD 函数，客户端别名是本机展开，两者并存。

## 验收
- [ ] 定义/编辑/删除别名
- [ ] 输入别名前缀展开为命令
- [ ] 别名可随账号持久化